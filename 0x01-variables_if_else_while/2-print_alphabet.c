#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/*Write a program that prints the alphabet in lowercase, followed by a new line. */
int main()
{
char a;
	for (a = 'a'; a <= 'z'; a++){
		putchar(a);
}
putchar('\n');
	return (0);
	}

