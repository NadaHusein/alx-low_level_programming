#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/*Write a program that prints the alphabet in lowercase, followed by a new line. */
int main()
{
        int x;
        for (x = 0; x < 10; x++){
                    printf("%d", x);
}
        printf("\n");   
return (0);
        }
