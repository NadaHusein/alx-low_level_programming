#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/*Write a program that prints the alphabet in lowercase, followed by a new line. */
int main()
{
char ch;
int x;
for(x = 48; x<= 58; x++){
	putchar(x);
}
	for (ch = 'a'; a <= 'f'; a++){
		putchar(a);
}
putchar('\n');
	return (0);
	}

