#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/*Write a program that prints the alphabet in lowercase, followed by a new line. */
int main()
{
int x;
	for (x = 48; x <= 57; x++){
		
		putchar(a);
		if(x !=57){
		putchar(',');
		putchar(' ');
		}
}
putchar('\n');
	return (0);
	}

